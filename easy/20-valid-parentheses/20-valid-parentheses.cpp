                    return false;
                    ) {
                    solver.top() == '{' && s[i] != '}' 
                    solver.top() == '[' && s[i] != ']' ||
                    solver.top() == '(' && s[i] != ')' ||
                if (
            } else {
                solver.push(s[i]);
            if (s[i] == '(' || s[i] == '[' || s[i] == '{') {
        for(int i = 0; i < s.length(); i++) {
                    solver.empty() ||
        stack<char> solver;
                } else {
                    solver.pop();
                }
            }   
        }