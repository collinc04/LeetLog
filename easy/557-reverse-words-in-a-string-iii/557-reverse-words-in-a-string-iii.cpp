                    right --;
                    hold = s[right];
                    s[right] = s[left];
                    s[left] = hold;
                while(right > left) {
                    right = i - 1; 
            if(s[i] == ' ' || i == s.size() - 1){
        for(int i = 0; i < s.size(); i++) {
        left = 0;
                if(i == s.size() - 1) {
                    right = i;
                } else {
                }